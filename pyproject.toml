[tool.black]
target-version = ["py314"]
extend-exclude = "tools/brickemu"
force-exclude = "tools/brickemu"

[tool.pyright]
include = ["recompile", "tools"]
exclude = ["tools/brickemu/**/*.py"]
pythonVersion = "3.14"
typeCheckingMode = "strict"
